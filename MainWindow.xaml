<Window x:Class="WPF_TodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список задач" Height="450" Width="400">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📝 Мои задачи" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

            
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="TaskInput" Width="280" Height="30" FontSize="14" Padding="5"/>
            <Button Content="Добавить" Width="90" Height="30" Margin="5,0,0,0" Click="AddTask_Click"/>
        </StackPanel>

        
        <ListBox x:Name="TaskList" Grid.Row="2" FontSize="14"/>

        
        <Button Grid.Row="3" Content="Удалить выбранное" Height="35" Margin="0,10,0,0" Click="DeleteTask_Click"/>
    </Grid>
</Window>